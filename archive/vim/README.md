# Archived Vim assets

이 디렉터리는 과거에 함께 배포하던 Vim 자료를 기록 보존 목적으로 보관합니다.
`install.sh`가 연결하는 활성 런타임은 `editor/.vim/`이며, 여기의 파일은 설치하거나
로드하지 않습니다.

- `colors/`: 현재 사용하지 않는 레거시 컬러스킴
- `autoload/pathogen.vim`: `vim-plug`으로 교체된 플러그인 로더
- `plugin/`: 과거 컬러스킴 선택용 플러그인

필요한 파일은 내용을 검토한 뒤 활성 런타임으로 명시적으로 복원하세요.
